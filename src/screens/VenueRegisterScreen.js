import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const VenueRegisterScreen = () => {
  return <View>
    <Text>Venue Register Screen</Text>
  </View>
}

const styles = StyleSheet.create({

})

export default VenueRegisterScreen;
